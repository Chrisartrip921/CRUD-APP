const User = require('../../models/User');
module.exports = {
    Mutation: {
        //args is RegisterInput from typeDefs.js
        register(_, args, context, info) {
            
        }
    }
}